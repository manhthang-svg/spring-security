spring:
    flyway:
        baseline-on-migrate: true
        baseline-version: 0
        enabled: true
    jpa:
        hibernate:
            ddl-auto: validate
        properties:
            hibernate:
                format_sql: true
        show-sql: false
    profiles:
        active: local

